<tr>
	<td class="uk-text-nowrap">
		<p class="uk-margin-remove sc-text-semibold">{{ name }}</p>
		<span class="sc-color-secondary">{{ description }}</span>
	</td>
	{{#intl locales=parent.invoice.locales }}
	<td class="uk-text-right">{{ formatNumber price style="currency" currency=parent.invoice.currency }}</td>
	<td class="uk-text-center">{{ quantity }}</td>
	<td class="uk-text-center">{{ VAT }}%</td>
	<td class="uk-text-right"><span class="sc-text-semibold">{{ formatNumber total style="currency" currency=parent.invoice.currency }}</span></td>
	{{/intl}}
</tr>
